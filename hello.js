const mission = process.argv[2];    // index 2 means in terminal whtever u  pass in 2nd index it will get printed 

if(mission ==='learn'){
   console.log('Time to write some code');

}else{
    console.log(`Is ${mission} really was fun?`);
}